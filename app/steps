# 📋 **Complete Guide: Philippine Diabetes Risk Predictor by Ampiyas Ⓣ**

Here are all the steps, apps, and commands you need to recreate or continue your project:

## 🛠️ **Apps You Need Installed**

| App | Where to Get It | Purpose |
|-----|-----------------|---------|
| **Python** | [python.org](https://www.python.org/downloads/) | Programming language |
| **Git** | [git-scm.com](https://git-scm.com/download/win) | Version control |
| **GitHub Account** | [github.com](https://github.com) | Store code online |
| **Render Account** | [render.com](https://render.com) | Deploy app live |
| **Code Editor** | [vscode.dev](https://vscode.dev) or Notepad | Edit code |

## 🚀 **Complete Setup Steps**

### **STEP 1: Open Git Bash or Command Prompt**
```bash
# Press Windows Key, type "Git Bash" or "cmd"
```

### **STEP 2: Create Project Folder**
```bash
cd C:\Users\%USERNAME%\Desktop
mkdir ph-diabetes-predictor
cd ph-diabetes-predictor
```

### **STEP 3: Create Virtual Environment**
```bash
python -m venv venv
venv\Scripts\activate
```

You should see `(venv)` at the beginning of your command line.

### **STEP 4: Create Requirements File**
```bash
notepad requirements.txt
```

Copy and paste:
```txt
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
seaborn>=0.12.0
flask>=2.3.0
flask-cors>=4.0.0
gunicorn>=20.1.0
python-dotenv>=1.0.0
```

Save (Ctrl+S) and close.

### **STEP 5: Install Requirements**
```bash
pip install -r requirements.txt
```

### **STEP 6: Create Project Folders**
```bash
mkdir app src saved_models data tests
mkdir app\templates
```

### **STEP 7: Create .gitignore**
```bash
notepad .gitignore
```

Copy and paste:
```txt
# Virtual Environment
venv/
env/
ENV/
myenv/

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
*.so
*.dll
*.egg
*.egg-info/
dist/
build/

# PyTorch
*.pth
*.pt
checkpoints/
saved_models/
!saved_models/diabetes_model_fixed.pth

# Data
data/
*.csv
*.xlsx

# IDE
.vscode/
.idea/
*.swp
*.swo

# Jupyter
.ipynb_checkpoints/

# Environment variables
.env
*.log
```

Save and close.

### **STEP 8: Create Model File**
```bash
notepad src\model.py
```

Copy and paste:
```python
import torch
import torch.nn as nn
import torch.nn.functional as F

class DiabetesPredictor(nn.Module):
    def __init__(self, input_size=8, hidden_sizes=[16, 8], output_size=1):
        super(DiabetesPredictor, self).__init__()
        
        self.fc1 = nn.Linear(input_size, hidden_sizes[0])
        self.bn1 = nn.BatchNorm1d(hidden_sizes[0])
        
        self.fc2 = nn.Linear(hidden_sizes[0], hidden_sizes[1])
        self.bn2 = nn.BatchNorm1d(hidden_sizes[1])
        
        self.fc3 = nn.Linear(hidden_sizes[1], output_size)
        
        self.dropout = nn.Dropout(0.3)
        
    def forward(self, x):
        x = self.fc1(x)
        x = self.bn1(x)
        x = F.relu(x)
        x = self.dropout(x)
        
        x = self.fc2(x)
        x = self.bn2(x)
        x = F.relu(x)
        x = self.dropout(x)
        
        x = torch.sigmoid(self.fc3(x))
        return x
```

Save and close.

### **STEP 9: Create Data Downloader**
```bash
notepad download_data.py
```

```python
import pandas as pd
import os

os.makedirs('data', exist_ok=True)

url = "https://raw.githubusercontent.com/jbrownlee/Datasets/master/pima-indians-diabetes.data.csv"

column_names = [
    'Pregnancies', 'Glucose', 'BloodPressure', 'SkinThickness',
    'Insulin', 'BMI', 'DiabetesPedigreeFunction', 'Age', 'Outcome'
]

print("📥 Downloading diabetes dataset...")
df = pd.read_csv(url, names=column_names)
df.to_csv('data/diabetes.csv', index=False)
print(f"✅ Dataset downloaded to data/diabetes.csv")
print(f"   Shape: {df.shape}")
```

Run it:
```bash
python download_data.py
```

### **STEP 10: Create Training Script (Definitive Fix)**
```bash
notepad src\train_definitive_fix.py
```

Copy and paste the complete training script from our earlier message (the long one with all the steps). Then run:
```bash
python src\train_definitive_fix.py
```

### **STEP 11: Create Flask App**
```bash
notepad app\app.py
```

Copy and paste the complete app.py code from our earlier message. Then save.

### **STEP 12: Create HTML Templates**

**Index.html:**
```bash
notepad app\templates\index.html
```

**Result.html:**
```bash
notepad app\templates\result.html
```

### **STEP 13: Run Your App Locally**
```bash
python app\app.py
```

Open browser to: `http://127.0.0.1:5000`

### **STEP 14: Save to GitHub**

```bash
# Initialize git
git init

# Add all files
git add .
git commit -m "Initial commit: Philippine Diabetes Risk Predictor by Ampiyas"

# Connect to GitHub
git remote add origin https://github.com/ampiyas10/ph-diabetes-predictor.git
git branch -M main
git push -u origin main
```

### **STEP 15: Deploy to Render**

1. Go to [render.com](https://render.com)
2. Sign up with GitHub
3. Click "New +" → "Blueprint"
4. Select your repository
5. Click "Apply"
6. Wait 2-3 minutes
7. Get your live URL: `https://ph-diabetes-predictor.onrender.com`

## 📝 **Quick Commands to Start Your App (Daily Use)**

When you come back to work on it:

```bash
# Open Git Bash or Command Prompt
cd C:\ph-diabetes-predictor
venv\Scripts\activate
python app\app.py
```

## 🎯 **Your Important Links**

- **GitHub Repository:** `https://github.com/ampiyas10/ph-diabetes-predictor`
- **Live App:** `https://ph-diabetes-predictor.onrender.com`
- **Local App:** `http://127.0.0.1:5000`

## ✅ **Project Structure**
```
C:\ph-diabetes-predictor\
├── app/
│   ├── app.py
│   └── templates/
│       ├── index.html
│       └── result.html
├── src/
│   ├── model.py
│   └── train_definitive_fix.py
├── saved_models/
│   └── diabetes_model_fixed.pth
├── data/
│   └── diabetes.csv
├── venv/
├── requirements.txt
├── .gitignore
└── download_data.py
```

**Congratulations! Your Philippine Diabetes Risk Predictor by Ampiyas Ⓣ is complete!** 🎉
